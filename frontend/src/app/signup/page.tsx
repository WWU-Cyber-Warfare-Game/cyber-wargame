import SignUpPanel from "@/components/LogInPanel";

/**
 * The sign up page.
 * @returns 
 */
export default function SignUp() {
    return (
        <main>
            <h2>Sign Up</h2>
            <SignUpPanel signup />
        </main>
    );
}