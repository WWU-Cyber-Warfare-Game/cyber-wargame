import LogInPanel from "@/components/LogInPanel";

/**
 * The log in page.
 * @returns 
 */
export default function LogIn() {
    return (
        <main>
            <h2>Log In</h2>
            <LogInPanel /><br />
        </main>
    );
}